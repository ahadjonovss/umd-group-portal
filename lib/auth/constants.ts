// proxy.ts ham, server kodi ham import qiladi — faqat oddiy konstantalar (firebase import yo'q).
export const SESSION_COOKIE = "umd_session";

// Session cookie amal qilish muddati (14 kun).
export const SESSION_MAX_AGE_MS = 14 * 24 * 60 * 60 * 1000;
